# 3D-Cam Project Dependencies
# Compatible with NumPy 2.x

# Core numerical and scientific computing
numpy>=2.0.0,<3.0.0
# pip will try to use pre-built wheels when available
scipy>=1.11.0

# Computer vision
opencv-python>=4.8.0

# Raspberry Pi camera stack (install Picamera2 + libcamera via apt; see README)
simplejpeg>=1.6.5
pyopengl>=3.1.7
pyqt6>=6.6.0

# Visualization (matplotlib 3.9+ supports NumPy 2.x)
matplotlib>=3.9.0

# Image processing (for watershed segmentation in detect_pairs_watershed.py)
scikit-image==0.24.0

# Note: Tkinter is part of Python standard library (no pip install needed)
# Python 3.7+ is required

